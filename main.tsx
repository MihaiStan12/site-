import React from 'react';
import ReactDOM from 'react-dom/client';

const root = document.getElementById('root');

if (root) {
  ReactDOM.createRoot(root).render(
    <React.StrictMode>
      <h1>Bine ai venit la aplicația de timp de așteptare!</h1>
    </React.StrictMode>
  );
}
